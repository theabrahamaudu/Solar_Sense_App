from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='An app which allows users to get an estimated Solar PV system design specification and price esimate based on inputed energy needs.',
    author='Abraham Audu--Periva Energy Systems',
    license='MIT',
)
